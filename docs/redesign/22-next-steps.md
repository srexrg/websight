# 22 - Next Steps: Implementation Handoff

> **Who this is for**: the next agent (or developer) picking up the WebSight redesign. The research and planning phase is complete. This file tells you exactly what exists, what to read, what to build first, and how to work. Start here.

## Current state of the repo

- Branch `revamp/foundation-landing` carries the new emerald landing page (already built: `components/landing/*`, `lib/landing/content.ts`, interactive 3D globe). Nothing from the redesign plans has been implemented yet.
- The **old product is still live and untouched**: `public/tracker.js` -> `app/api/track/route.ts` -> `visits`/`page_views`/`daily_stats` tables, dashboard at `app/(root)/website/[domain]` via `components/analytics/AnalyticsClient.tsx` and `lib/actions/analytics.ts`.
- Package manager is **npm** (`package-lock.json`). There is **no `supabase/` directory** - all existing SQL (tables, RPCs like the analytics aggregate functions) lives only in the Supabase dashboard. Recovering it into version-controlled migrations is part of the first task.
- The 8-screen dashboard mock to build toward is `design/WebSight.dc.html`; tokens and rules in `design/design-system.md` and `app/globals.css`.
- All plan files (00-24) live in `docs/redesign/`. Read `00-research-and-strategy.md` first for shared context, then only the plan you are implementing - each is self-contained. `24-session-replay.md` was added 2026-07-06 (phase 4; depends on `01`, `02`, `05`, `07` - all done, so it is buildable now, but 09-11 remain the recommended next slice).
- **Infrastructure decision made** (`23-infrastructure-decision.md`): stay on Supabase; do not migrate. Native partitioning + pg_cron (pg_partman is NOT available on Supabase - do not use it), add query-timing logs in `queries.ts`, and revisit a ClickHouse split only for the events store if the triggers in `23` fire.

## Build order

Phase 1 is strictly sequential-ish and everything else depends on it. Recommended order **02 -> 01 -> 03**:

1. **`02-ingestion-and-data-model.md`** - the foundation. New `sites`, `salts`, `events`, `sessions`, `rollup_daily` schema, the atomic `ingest_event` RPC, and `lib/analytics/queries.ts` as the single query choke point. Nothing else can merge before this.
2. **`01-tracking-sdk.md`** - the new `<3KB` tracker in `packages/tracker/`, built against the `02` payload schema and ingest endpoint.
3. **`03-dashboard-shell-and-design-system.md`** - the sidebar app shell, route restructure to `app/(app)/[site]/(dashboard)/*`, dark mode, TanStack Query + nuqs. This creates the frame every Phase 2+ screen mounts into.

After Phase 1, files within a phase are independent and parallelizable. Sensible next sprint: `04` (overview) + `05` (filters) together - they share components and `05`'s filter model must be in `04`'s fetchers from the start.

## Step 0 - Dependency refresh & upgrade policy (decided 2026-07-03)

Checked via `npm outdated`. The core stack is already on the latest majors - Next.js 16.2, React 19.2, TypeScript 6, Tailwind v4.3, ESLint flat config - so **no framework migration is needed**. Policy going forward: the redesign is built on latest stable everything; run `npm outdated` at the start of each phase and take minors/patches immediately, majors deliberately. New dependencies introduced by the plans are always added at their latest stable version.

Concrete actions, ordered (do 1-3 before starting plan `02` code, the rest land inside the plans noted):

1. **Safe sweep now**: `npm update` for all minor/patch drift (Next 16.2.10, Tailwind 4.3.2, Radix, framer-motion 12.42, ua-parser-js, date-fns, tailwind-merge, eslint-config-next, etc.). Verify `npm run build`.
2. **Supabase clients** (drifted badly): `@supabase/supabase-js` 2.49 -> 2.110 and `@supabase/ssr` 0.6 -> 0.12. The 0.x ssr jump can change cookie handling - touches `utils/supabase/*` and `middleware.ts`; verify the Google login flow end-to-end after. Must happen before any new auth-adjacent code is written.
3. **ESLint 9 -> 10**: flat config is already in place, low risk.
4. **Recharts 2 -> 3** (major): upgrade now and build every new dashboard component (`03`/`04`+) on v3 from day one - do not build new UI on v2 and migrate later. Only 3 legacy files use it (`components/ui/chart.tsx`, `AnalyticsOverview.tsx`, `PageAnalytics.tsx`); patch them minimally since they're retired at the end of Phase 1 anyway.
5. **Node types**: `@types/node` is 20 but local Node is 25. Pin `"engines"` in package.json to the version the deploy target runs (Vercel: check project setting, likely 22 LTS), and match `@types/node` to it - don't blindly jump to 26.
6. **Icon consolidation** (during `03` theme migration): the repo ships three icon libraries. Phosphor is the standard (`components.json`, landing page); remove `lucide-react` (~13 files: shadcn ui defaults + old authed pages, all being restyled in `03` anyway) and `react-icons` (1 file: `login-button.tsx`).
7. **Don't upgrade, remove**: `jspdf`/`jspdf-autotable` (2.x, latest 4.x) go away when `19` retires PDF export - if `03`'s restructure orphans them earlier, delete earlier. `react-syntax-highlighter` (15 -> 16): replace with `shiki` during the `17` docs/MDX revamp instead of upgrading.
8. `@vercel/analytics` 1 -> 2: trivial, take it in the sweep (and decide during `03` whether dogfooding WebSight makes it redundant).

## First task, concretely (start of plan 02)

1. Initialize migrations: create `supabase/migrations/` (Supabase CLI), and snapshot the **existing** schema (tables + the RPCs currently only in the dashboard) as migration `0000_baseline` so the old system is reproducible before touching anything.
2. Migration `0001`: the new schema from `02` (sites, salts, events partitioned by month, sessions, rollup_daily) plus the `ingest_event` RPC and pg_cron salt rotation.
3. New ingest route (per `02`) writing through `ingest_event`. Keep the old `/api/track` running - **dual-write from the old route into the new tables** during transition so no data is lost and the new dashboard has history from day one.
4. Backfill `visits`/`page_views`/`daily_stats` history into `events`/`sessions`/`rollup_daily` (script spec is in `02`).
5. Implement `lib/analytics/queries.ts` with the first two fetchers (metrics + timeseries) and prove them against backfilled data.

Do not cut over or delete anything old until `03`'s shell renders the new overview from the new tables. Old dashboard removal is the *last* step of Phase 1, not the first.

## Working agreements

- **Branches**: one branch per plan file, named `redesign/02-ingestion`, `redesign/01-tracker`, etc., PRs into `main` (or into a long-running `redesign` integration branch if `main` must stay deployable - decide with the user at kickoff).
- **Milestones are the unit of work**: every plan ends with numbered Development milestones; implement and verify them in order. A plan is "done" when all its milestones pass, its edge-case list has been walked, and `npm run build` + lint are clean.
- **Schema changes only via `supabase/migrations/`** from now on - never the dashboard.
- **Design rules**: emerald `#0E9C6E`, Hanken Grotesk for UI, JetBrains Mono for every number, light + dark from the tokens in `app/globals.css`. No em-dashes in any user-facing copy (user preference: plain dashes).
- **Every screen ships with empty, loading, and error states** - the plans call this out repeatedly; treat it as a merge requirement.
- **Cross-plan references**: plans cite each other by number (e.g. "the `05` URL codec"). If you implement out of order and hit a dependency, stub behind an interface and note it in this file rather than pulling the whole other plan forward.

## Environment / accounts needed along the way

- Now (Phase 1): Supabase project access (already in `.env.local`), Supabase CLI, pg_cron enabled.
- Later: Resend key (`16`/`19`), Stripe account + `BILLING_ENABLED` (`18`), `ANTHROPIC_API_KEY` (`20`), payment-provider test accounts (`21`). Each plan lists its own env vars.

## Known caveats from the research phase

- Rybbit/Plausible/PostHog findings in the plans were adversarially verified; **Sleek claims are site-sourced and directional only** - don't treat Sleek feature descriptions as ground truth if you go re-research.
- Refuted claims already excluded: Rybbit's "18KB script" (real figure ~9.3KB transfer/26KB decoded), Rybbit RBAC/password-protected sharing (our `15`/`16` designs are original, not copied fact).

## Status tracker

Update this table as work proceeds so any future agent knows where things stand.

| Plan | Status | Branch / PR | Notes |
|---|---|---|---|
| Step 0 deps refresh | **done** (2026-07-03) | `redesign/02-ingestion` | items 1-3 done. ESLint stays on 9: `eslint-config-next` 16.2.10 bundles an `eslint-plugin-react` that crashes under ESLint 10 (`getFilename` removed) - retry when Next ships compat. `@supabase/ssr` 0.12 verified compiling + building; Google login flow still needs a manual e2e check against prod before deploy. |
| 01 tracking-sdk | **milestones 1-4 done** (2026-07-05) | `redesign/01-tracker` | `packages/tracker` -> `public/t.js` (tsup IIFE, **1.8KB gzipped** vs 3KB budget, enforced by a test) + lazy `public/t-x.js` (web-vitals + error capture, loaded only with `data-vitals`/`data-errors`). Full feature set: SPA pageviews (history patch + dedupe), batched sendBeacon/fetch transport (first event flushes immediately), outbound/download/form/`data-ws-event` auto-capture, `websight.track/identify` + pre-init stub queue, stateless/persistent modes, client-side query stripping (utm/click-ids kept), localhost + `websight_ignore` + opt-in DNT guards, prerender/bfcache handling. 19 jsdom unit tests run the BUILT bundle; `tests/integration/sdk.test.ts` replays real beacons through the route into the DB. Snippet modal + self-tracking switched to `/t.js` (`prebuild` regenerates). Legacy `public/tracker.js` left untouched - switching embedded sites to `/t.js` stops legacy-table writes, so migrate embeds only after `03` renders from new tables. **Remaining**: milestone 5 (`@websight/js` npm publish, per-framework docs tabs). Proxy recipe docs written 2026-07-09 (`docs/proxy-guide.md`: first-party `/stats/*` prefix + `data-api`, Next/Vercel/Netlify/Cloudflare/Nginx/Caddy rules). |
| 02 ingestion-and-data-model | **milestones 1-4 done** (2026-07-03) | `redesign/02-ingestion` | Migrations `0000` (legacy baseline reconstruction) + `0001` (sites/salts/events/sessions/rollup_daily, `ingest_event`, read RPCs, pg_cron jobs). New `/api/track` handles v2 batches + legacy dual-write; `/api/events` dual-writes sessionless. `lib/analytics/queries.ts` (overview/timeseries/breakdown) proven against backfilled data. `scripts/backfill.ts` written + verified. **Deployed to prod 2026-07-05**: migrations applied, pg_cron jobs active, backfill run, 40 tests green against the cloud project (integration tests target `.env.local` cloud creds, local stack as fallback). **Remaining**: Vercel env vars + app deploy (see notes below); milestone 5 (rate limiting, per-site origin validation, monitoring); cut-over/retire old tables waits for `03`. |
| 03 dashboard-shell | **milestones 1-4 done** (2026-07-05) | `redesign/01-tracker` | New `(app)` route group: emerald sidebar shell (248px, collapsible/persisted, Phosphor icons, site switcher with favicons), topbar with nuqs range tabs (24h/7d/30d/90d), screens Overview/Pages/Sources/Audience/Events/Site-Settings on the v2 tables via session-authed `GET /api/analytics/[site]` (RLS ownership) + TanStack Query; `/dashboard` sites grid with 7d sparklines + add/delete site (writes `sites` AND legacy `domains`); account settings restyled at `/settings`. Dark mode finished (`#0E1310`-family, emerald kept, charts re-theme via CSS vars). Recharts upgraded to v3; all new charts on it. Old dark/indigo authed UI DELETED (AnalyticsClient/AnalyticsOverview/PageAnalytics/WorldMap/ExportButton/header/chart.tsx + jspdf/react-simple-maps deps); `/website/[domain]` 308-redirects to `/[site]/overview`. Migration `20260705130000` adds `analytics_event_breakdown` RPC (applied to prod). Unbuilt screens hidden via `lib/dashboard/nav.ts` flags. Smoke-tested live in Chrome against prod data (all screens, dark mode, range tabs, RLS isolation, legacy redirect). **Remaining**: milestone 3's `/design` demo route skipped; milestone 5 mobile bottom-sheet nav (sidebar hidden < md for now) + `user_prefs` DB persistence (localStorage used instead); keyboard shortcuts deferred to `05`. |
| 04 overview-dashboard | **milestones 1-4 done** (2026-07-05) | `redesign/01-tracker` | Six metric cards (visitors/sessions/pageviews/views-per-session/bounce/duration) with delta chips, metric-switchable main chart with dashed comparison series + dual-period tooltip; tabbed breakdown grid (Pages Top/Entry/Exit, Sources Channels/Referrers/Campaigns, Locations Countries/Regions/Cities, Devices Browser/OS/Device, Custom Events); comparison modes off/previous-period/YoY via `?compare=` (comparison period always gets identical filters); zero-previous shows no delta ("new"). **Remaining (M3/M5)**: full date-range picker with custom dates + day-of-week alignment + persisted preference, details modal with search/CSV, goals summary card (blocked on `08`), live pill (blocked on `06`), mobile pass. |
| 05 filtering-and-segmentation | **milestones 1-3 done** (2026-07-05) | `redesign/01-tracker` | Canonical filter model + URL codec (`?f=country:is:US,DE;path:contains:/blog`, reserved chars escaped, prop dims supported); SQL translation lives in `_analytics_where` (migration `20260705150000`, applied to prod) - dim whitelist + quote_literal, ops is/is_not/contains/not_contains, session-scoped entry/exit dims, prop:<key>; all `analytics_*` read RPCs take `p_filters`; `analytics_dimension_values` powers type-ahead ranked by traffic. Click-to-filter on every breakdown row (active rows highlighted, not hidden), filter pills + Clear all + Esc, filter editor (dimension/operator/value with live suggestions), filters follow navigation across screens and compose with comparison. `segments` table created (schema only). Verified: 20 unit tests (codec/comparison) + 10 integration tests incl. live SQL-injection check, browser smoke on prod data. **Remaining (M4/M5)**: saved segments CRUD + UI, segment-size annotation, mobile filter sheet, `f` keyboard shortcut. |
| 06 realtime-and-globe | **milestones 1-4 done** (2026-07-05) | `redesign/01-tracker` | Migration `20260705170000` (applied to prod): `analytics_live_count`/`analytics_live_breakdown` (5-min window, filter-aware), minute granularity, `sessions(site_id,last_event_at)` index. Live pill on every screen (15s poll). Realtime screen: big count, per-minute chart (30 min), active pages/referrers/countries with click-to-filter, activity feed (5s poll, id cursor, hover-freeze, time-ordered). Globe screen: **MapLibre GL v5 globe projection + OpenFreeMap `liberty` tiles** (free, no API key, self-hostable) - real labeled cartography, pulsing count markers per country (centroid-based), marker click-to-filter, auto-spin (pauses on interaction, off under reduced motion), live/range modes, Countries/Regions/Cities panel. The three-globe stays on the landing only. **Update (2026-07-05)**: globe restyled off OpenFreeMap `liberty` (OSM watermark) to a self-hosted public-domain Natural Earth basemap in a light rybbit/Mapbox-Standard palette (light-blue ocean/lakes, soft land, atmosphere halo, starfield) - no tiles, no API key, no attribution/watermark; `.geojson` excluded from auth middleware for fast static serving. **Remaining (M5)**: city-level markers (store rounded lat/lng at ingest), 2D/WebGL fallback, ticker sampling at high traffic. |
| 07 sessions-and-user-profiles | **milestones 1-5 done** (2026-07-05) | `redesign/01-tracker` | Migrations (pushed to prod): `20260705190000_sessions_list` (`analytics_sessions_list` keyset RPC + `sessions(site_id,started_at desc,id desc)` index), `20260705200000_session_events` (`analytics_session_events`), `20260705210000_profiles` (`profiles` table + `sessions(site_id,visitor_id,started_at desc)` index + **exception-safe AFTER-INSERT trigger** on `events` capturing `identify()` traits + 4 profile RPCs). **Sessions** screen: keyset infinite-scroll list (filter+range aware via events-EXISTS), row → **drawer** (Radix `Sheet`) with full event timeline (pageviews title+path, custom events with expandable JSON props, idle-gap markers, LIVE polling for open sessions). **Profiles** list/detail (persistent-mode): lifetime aggregates merged by user_id else visitor_id, `identify()` traits card, scoped sessions + event-frequency. **Privacy degradation**: stateless sites show an explainer (list + deep-link guarded, no 404). **GDPR**: `DELETE /api/sites/:id/visitors/:visitorId` erases events+sessions+profiles (owner-only, confirm dialog). **Polish**: deterministic visitor identicon colors + codes, CSV export of the loaded session list, prefetch-on-hover for instant drawer. Testing moved OFF prod site data to a seeded throwaway site (`scripts/seed-demo.mts`, "Seed Demo"). Verified end-to-end in-browser (list/pagination/filter, drawer/timeline/props, profiles/search/detail/traits, stateless explainer, GDPR delete → 0 rows). **Remaining**: none for the plan; nice-to-haves per plan's "future improvements" (session replay - now its own plan `24`, cohort-save). |
| 08 goals-and-conversions | **milestones 1-4 done** (2026-07-05) | `redesign/01-tracker` | Migrations (pushed): `20260705220000_goals` (goals table + `_goal_where` compiler + `analytics_goal_preview`/`_stats`), `20260705230000_goal_stats` (`analytics_goals_with_stats` + `analytics_goal_timeseries`), `20260705240000_goal_filter_dim` (`_goal_session_subquery` + `_analytics_where` gains a `goal` dim; immutable->stable). Goals are query-time definitions (Plausible model): page goals (exact/contains/wildcard LIKE) + event goals (name + prop conditions via 05 model). **Goals screen**: table with type chip, uniques/conversions/rate (denominator on hover), per-row sparkline, create/edit dialog (page/event, prop editor, value), archive (soft-delete). **Goal-as-filter**: `dim:'goal'` scopes any screen to a goal's converters (verified regression-clean: existing dims unchanged; goal filter exact vs ground truth). **Overview**: top-5 Goals summary card. **Goal detail** (`/goals/[id]`): stat tiles, conversion bar chart, top countries/referrers for converters, recent converting sessions (drawer). **M4**: live match preview ("would have matched N events from M visitors in 7d", debounced), empty-state templates. CRUD routes `/api/sites/:id/goals[/:id][/preview]` (owner-only via `resolveOwnedSite`). Verified end-to-end in-browser + service-role scripts. **Remaining**: live "test goal" listener deferred (match preview covers definition-correctness; needs live traffic to demo). |
| 09 funnels | **milestones 1-5 done** (2026-07-05) | `redesign/01-tracker` | Migrations (pushed): `20260705250000_funnels` (`funnels` table + `_funnel_step_where` + `analytics_funnel` sequential lateral-join compiler), `20260705260000_funnel_drilldown` (`_funnel_base_sql` + `analytics_funnel_step_visitors`), `20260705270000_funnel_ttc` (`analytics_funnel_ttc`). Query-time sequential funnels (window from step 1); steps are page/event/saved-goal, reusing the 08 goal compiler. **Verified exact vs hand-computed ground truth** (45->11->3; window + ordering; drill-down converted/dropped counts). Editor (steps, reorder, window, live 7-day preview), CRUD + preview routes, list with per-funnel results. Detail: step bar chart, %-of-first, drop-off in --danger, median-to-convert inline, clickable **step drill-down** (dropped/reached visitor lists linking to profiles/sessions), **time-to-convert histogram**, and period-comparison per step (`?compare=`). Global filter bar flows through `computeFunnel`. **Stateless guard**: window capped at 1 day in the editor. Ordering v1 = sequential; strict/any-order + exclusion steps are future scope. |
| 10 user-journeys | **milestones 1-4 done** (2026-07-05) | `redesign/01-tracker` | Migration (pushed): `20260705280000_session_paths` (`analytics_session_paths` - per-session ordered pageview arrays, filter-aware). Journey aggregation is query-time in the route: `lib/analytics/journeys.ts` `computeJourneys` turns path sequences into Sankey nodes/edges (per-column instances so loops stay distinct), with anchor slicing (starts/ends), top-N/Other folding, per-node drop-off, wildcard grouping (`/blog/*`), and a top-paths tuple list. Verified vs ground truth incl. the conservation invariant (out-edges + ended = count). **Sankey** rendered with `d3-sankey` (new dep, ~4KB) + custom SVG (emerald flow, `--danger` "Exited" drop-off sinks, node/edge tooltips). Screen: starts/ends toggle, steps + top-N selectors, **node click-menu** (set as start/end, filter dashboard to page, view sessions -> 07), anchor chip, grouping-rule chips, **mobile top-paths list**, **PNG export** (CSS vars inlined). Global filter bar + range apply. Ordering/pages-only v1; event nodes + PNG-of-graph niceties are future scope. |
| 11 retention | **milestones 1-4 done** (2026-07-07) | `redesign/01-tracker` | Migration (pushed): `20260707000000_retention` (`analytics_retention` + `analytics_retention_visitors`). Query-time cohort retention (PostHog model): cohort = first-seen interval bucket per identity (`user_id` supersedes `visitor_id`), returns come from any-visit or a saved goal; entry likewise (reuses the 08 `_goal_where` compiler). Buckets emitted as `YYYY-MM-DD` wall-clock strings so `lib/analytics/retention.ts` `computeRetention` derives period index / in-progress bucket without tz math; fills the triangle, flags in-progress + low-sample, computes the size-weighted average. **Verified exact vs independent JS ground truth on the seed site** (sizes + per-cell counts, 0 mismatches; period-0 == cohort-size invariant). Grid (CSS-grid heatmap, per-grid-normalized emerald intensity, hatched low-sample, dashed in-progress, sticky labels, h-scroll), controls (day/week/month, periods, entry/return goal, % cohort / % prev basis), cell drill-down (visitor list -> profiles), cohort-decay overlays, global filters. Stateless sites get a `RetentionLocked` explainer. 10 unit tests + browser smoke (grid/drill-down/trend/basis) green. |
| 12 web-vitals | **milestones 1-4 done** (2026-07-07) | `redesign/01-tracker` | Migrations (pushed): `20260707010000_web_vitals` (regex-guarded `vital_value` generated column + partial index, `_vital_rating` thresholds helper, 5 `analytics_vitals_*` RPCs) + `20260707020000_vitals_numeric` (percentile_cont returns float8 -> cast every percentile `::numeric` so result columns + the numeric `_vital_rating` overload resolve). Ratings always recomputed from raw values, never stored `props.rating`. Tracker: `t-x.js` upgraded to `web-vitals/attribution` (element + loadState per metric) + `data-vitals="0.1"` sample rate through the core. `lib/analytics/vitals.ts` (summary/timeseries/pages/breakdown/attribution) + `PATCH /api/sites/:id/settings` enable toggle. Screen: metric cards (p75 + rating + CrUX distribution bar + thresholds), p75 timeseries with good/poor threshold bands, All/Desktop/Mobile control composing with global filters, device/browser/country breakdowns, sortable pages table (rating dots), cell -> attribution panel (top elements on slow loads), n<30 hiding, Safari INP/LCP + soft-nav coverage notes. **Verified exact vs independent JS ground truth** (`scripts/verify-vitals.mts`: p75 via percentile_cont interpolation + rating distributions + sample counts, all 5 metrics, 0 mismatches) + browser smoke (cards/timeseries/device/breakdowns/pages/attribution/coverage). `scripts/seed-vitals.mts` seeds 900 realistic loads (mobile + /signup slow, Safari omits INP/LCP). Enable-explainer + settings PATCH wired (not browser-tested - seed site has data). |
| 13 error-tracking | **milestones 1-4 done** (2026-07-07) | `redesign/01-tracker` | Migration (pushed): `20260707030000_error_tracking` (`error_groups` table, `_error_fingerprint`, BEFORE-insert `errors_capture` trigger, 6 read RPCs). Errors fingerprinted at ingest: normalize message (numbers/UUIDs/URLs) + top-3 script stack frames (strip host/query/content-hash/line:col) -> md5, so a bug groups across visitors and across bundle-hash deploys. Trigger writes fingerprint onto the event, upserts the group, **rate-caps 120/min/group** (`dropped`), **auto-reopens** resolved groups as `regressed`; exception-safe (never breaks ingestion). Tracker `t-x.js` errors chunk enhanced (type, off-origin `external` flag, 5/fingerprint/session client dedupe, 4KB stacks). Screens: list (Open/Ignored/Resolved/All tabs, message + type + visitors + browser + external chip + regressed badge + rate-limited note, triage menu), detail (stat tiles, occurrence bars, Pages/Browsers/OS/Countries breakdowns, **XSS-safe** escaped StackView, occurrences -> visitor profiles), Overview "N open errors 24h" indicator (hidden when healthy), celebratory zero-error empty state. `PATCH /api/sites/:id/errors/:groupId` triage. **Verified vs ground truth** (`scripts/verify-errors.mts`: 5 fingerprints=5 groups, number-varying template collapses to 1 group, occurrence counts, external flag, RPC range counts, 0 mismatches) + browser smoke (list/detail/triage/overview-indicator). **Deviation**: rate cap is per-group/min not per-site; occurrences link to visitor profile (no session-drawer deep-link URL yet - future). |
| 14 custom-events | **milestones 2,3,5 done; 1 pre-done (01/02); 4 deferred** (2026-07-07) | `redesign/01-tracker` | Migrations (pushed): `20260707040000_custom_events` (`_is_reserved_event`, `event_dictionary` + auto-upsert trigger, 5 `analytics_event_*` RPCs) + `20260707050000_event_prop_keys_fix` (prop_keys USING arg order). Custom events = all events with a non-reserved name (pageview/web_vital/error/identify). Events screen upgraded from flat list to **aggregated names table** (count/visitors/last-seen/description/stale badge) -> detail page (count-over-time bars, **property explorer**: sampled prop keys -> value breakdown with counts/% + high-cardinality guard + click-to-filter via `prop:<key>`, expandable-props occurrences -> profiles). Inline dictionary description edit + `expected_props` unexpected-prop nudge + 30d stale badge. `PATCH /api/sites/:id/event-dictionary`. `name`/`prop:` filter dims already existed (05). **Verified vs ground truth** (`scripts/verify-events.mts`: names count/visitors all exact, prop keys `value,sku`, prop values `SKU-42=12`, dictionary trigger fires on fresh insert) + browser smoke (names -> detail -> prop explorer -> click-to-filter `prop:sku:is:SKU-42`). M1 ingest (browser `track`/`data-ws-event`/legacy `message`->props) already shipped in 01/02. **M4 deferred**: server API v2 + site keys explicitly belong to plan 19 (`site_api_keys`); browser/no-code sending already works, and the interactive test-event panel + docs rewrite ride with 19's keys. |
| 15 public-dashboards | **milestones 1,2,4 done; 5 + OG deferred** (2026-07-08) | `redesign/01-tracker` | Migration (pushed): `20260708000000_share_tokens`. **No new analytics SQL** - public views reuse the exact member fetchers via `resolveSiteAccess` (the single auth gate in `/api/analytics/[site]`: session owner -> `member` scope; `?share=<token>` -> `share` scope). `shareAllows` gates share scope to non-sensitive kinds (overview/timeseries/breakdown/dimension-values always; live-* iff realtime/globe exposed; events/goals iff not hidden; sessions/profiles/funnels/journeys/retention/vitals/errors always blocked). Screens reuse **unchanged**: `ShareContext` -> `useDashboardParams` appends `?share=`, and OverviewScreen hides goals/errors/events cards under `useReadOnly`. Public `/share/[token]` shell (favicon + name + range tabs + exposed-screen tabs + Powered-by badge, noindex on secret links). Share CRUD `/api/sites/:id/share` (create/rotate/delete, visibility, exposed screens, hide_events, bcrypt password); `/api/share/:token/unlock` (bcrypt + unforgeable httpOnly cookie derived from the hash); `/api/share/:token/badge` (CORS JSON) + `public/embed.js` live-visitors pill. Share settings card in Settings. **Verified in browser + curl**: member dashboard still works post-refactor; anonymous no-share 401; share overview/breakdown 200; sessions/events/errors 403; bad token 404; password gate (page + API 403 -> unlock -> cookie -> 200, wrong pw 401); rotation kills the old token (404) while keeping the password; last_accessed tracking. **Deferred**: OG images (`@vercel/og`, M4) + WebSight's own public dashboard on the landing page (M5) - both polish, no dependency risk. |
| 17 onboarding | **milestones 1-4 done; 5 deferred** (2026-07-08) | `redesign/01-tracker` | **No migration** (checklist derives from data, not a stored flag). Guided first-run flow `/onboarding` (auto-redirect from `/dashboard` when zero sites): create-site step (domain normalize via `normalizeSiteKey`, timezone auto-detected via `Intl`, privacy-mode choice with plain-language explainers) -> install step (`InstallTabs`: 9 platforms - HTML/Next.js/React/Vue/WordPress/Shopify/Webflow/Framer/GTM, personalized snippet) -> verify step (`VerifyCard` polling `GET /api/sites/:id/install-status` every 3s, echoes the first received event: "We just saw a visit from 🇩🇪 Germany on Chrome at /welcome") -> dashboard. Overview gains a **getting-started checklist** (`GET /api/sites/:id/onboarding`, self-completing from data: install/pageview/goal/custom-event, hidden once mature) - member-only (`useReadOnly`). Settings "Installation" section reuses `InstallTabs`. `createSiteOnboarding` action returns public_id. **Verified end-to-end in browser**: messy URL normalized -> site created -> 9-platform install -> live verify flipped to success with country/browser/path echo on a seeded test event -> landed on Overview with checklist 2/4; throwaway site cleaned up. **Deferred**: MDX platform-guide docs pages (M3, snippets cover the 9 inline), demo-data entry (needs `DEMO_SITE_TOKEN`), email-snippet + 24h nudge (M5, need `lib/email/` from plan 16). **✅ UI polish pass done (2026-07-09)**: dedicated design pass landed, addressing every flagged item. New shared `components/onboarding/brand-split.tsx` two-pane shell (emerald brand panel echoing the landing - logo, "Analytics that respect your visitors" headline, trust ticks, ambient glow + concentric globe rings) wraps both the onboarding flow and the `components/share/*` password gate; collapses to a slim branded header on mobile. Onboarding flow rebuilt: labeled 3-step stepper with connecting fill, framer-motion step transitions (reduced-motion aware), privacy-mode as selectable Phosphor-iconed cards (ShieldCheck/Fingerprint, Recommended badge, emerald ring on select), domain input with inline globe icon. Install step: segmented platform pills + landing-grade dark code block (window chrome, filename label, emerald glow shadow, hover-brand copy button). Verify step: emoji replaced by Phosphor `Broadcast` (with `wsPulse` listening rings) / `CheckCircle` (spring reveal), echo shown in an accent callout. Password gate: Phosphor `LockKey`, "Powered by WebSight" framing. Getting-started checklist icons -> Phosphor. **Verified in browser (light + dark) via throwaway `/wspreview` route, since removed**; `npm run build` + lint clean. |
| 16, 18-21 | not started | - | 16 (orgs) intentionally skipped in user's slice; 18 billing next major |
| 24 session-replay | not started (plan written 2026-07-06) | - | phase 4; deps `01`/`02`/`05`/`07` all done - buildable anytime; needs an S3-compatible store (cloud default: Cloudflare R2, chosen for zero-egress playback) + `CRON_SECRET` |

### ⚠️ Security: legacy-table RLS leak (found 2026-07-10; fix APPLIED to prod, verified 2026-07-11)

Anon-key probe of the live project found the five legacy tables world-readable via
the public publishable key with RLS off: `users` (31 rows incl. **emails**),
`domains` (24), `visits` (2501), `page_views` (4729), `daily_stats` (767). Root
cause: baseline migration reproduces the old `grant ... to anon` with no RLS (the
old app read/wrote these with the anon key). All v2 tables are correctly
RLS-protected. Fix: `20260710000000_legacy_rls_lockdown.sql` (enable RLS + owner
policies; ingestion writes via service role so dual-write is unaffected).
**Verified 2026-07-11**: `supabase migration list --linked` shows `20260710000000`
applied, and an anon-key probe of all five legacy tables plus every v2 table
(sites/events/sessions/profiles/goals/funnels/share_tokens/salts/error_groups/
event_dictionary/segments) returns zero rows. Still to do: verify a fresh Google
login (auth callback inserts into `users` under the new self-insert policy).

### Deploy notes for `02` (prod rollout)

**Done on prod 2026-07-05**: project linked, baseline repaired as applied, `0001` pushed (pg_cron enabled, all three `ws-*` jobs active), backfill run (4,576 events / 2,321 sessions / 702 rollup days from 23 sites), parity spot-checked vs `daily_stats` (matches; where they differ the legacy numbers were undercounts from the non-atomic old route), full 40-test suite green against the cloud project. The project uses the **new API key system** (`sb_publishable_...` / `sb_secret_...`); all clients accept the new env names `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY` / `SUPABASE_SECRET_KEY` with the legacy names as fallback. Found on prod and folded back into the baseline migration: `public.users.id` references `auth.users(id)` and `users.email` is unique.

**App deployed to prod (confirmed 2026-07-11)**: Vercel env keys set and the redesign app is live at `websight.srexrg.me` - verified `/t.js` serves the new tracker, `/website/[domain]` redirects, `/api/track` accepts v2 (202). Remaining from the old checklist: confirm one fresh Google login e2e on prod (covers both the `@supabase/ssr` 0.12 + publishable-key switch and the new `users` self-insert RLS policy).
